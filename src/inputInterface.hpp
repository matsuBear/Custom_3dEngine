#pragma once

template <typename... Args> void primaryAction(Args...) {};

template <typename... Args> void secondaryAction(Args...) {};

template <typename... Args> void thirdAction(Args...) {};

template <typename... Args> void fourthAction(Args...) {};

template <typename... Args> void modPrimaryAction(Args...) {};

template <typename... Args> void modSecondaryAction(Args...) {};

template <typename... Args> void modThirdAction(Args...) {};

template <typename... Args> void mouseMoveAction(Args...) {};

template <typename... Args> void rightAction(Args...) {};

template <typename... Args> void leftAction(Args...) {};

template <typename... Args> void upAction(Args...) {};

template <typename... Args> void downAction(Args...) {};

template <typename... Args> void forwardAction(Args...) {};

template <typename... Args> void backwardAction(Args...) {};

template <typename... Args> void modForwardAction(Args...) {};

template <typename... Args> void modBackwardAction(Args...) {};
